# TEMPLATE ONLY: do not apply or put real values in this tracked file.
# Copy it to an ignored .env workflow or create the Secret imperatively from a
# secure secret manager. Kubernetes Secrets are encoded, not encrypted by this
# manifest; production should enable encryption at rest and external rotation.
apiVersion: v1
kind: Secret
metadata:
  name: housing-elt-snowflake
  namespace: housing-elt
type: Opaque
stringData:
  HOUSING_ELT_SNOWFLAKE_ACCOUNT: REPLACE_ME
  HOUSING_ELT_SNOWFLAKE_USER: REPLACE_ME
  HOUSING_ELT_SNOWFLAKE_PASSWORD: REPLACE_ME
  HOUSING_ELT_SNOWFLAKE_WAREHOUSE: REPLACE_ME
  HOUSING_ELT_SNOWFLAKE_DATABASE: REPLACE_ME
  HOUSING_ELT_SNOWFLAKE_SCHEMA: REPLACE_ME
  HOUSING_ELT_SNOWFLAKE_ROLE: REPLACE_ME_OPTIONAL
