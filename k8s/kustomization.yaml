apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - base

# The tracked Snowflake Secret is deliberately not a resource. Applying this
# directory can never create placeholder credentials or contact Snowflake.
